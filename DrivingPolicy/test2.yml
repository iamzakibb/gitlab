stages:
  - debug

variables:
  HTTP_PROXY: $HTTP_PROXY
  HTTPS_PROXY: $HTTPS_PROXY
  NO_PROXY: $NO_PROXY

default:
  tags:
    - linux
    - docker

debug:
  stage: debug
  script:
    - echo "Proxy variables"
    - env | grep -i proxy
    - curl -I https://$CI_SERVER_HOST